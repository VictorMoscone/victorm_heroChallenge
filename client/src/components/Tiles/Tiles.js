import React from 'react';
import xmenImg from '../../images/xmen.png';

const Tiles = (props) => {

    return (
        <div>
            {props.team}
        </div>
    );
};

export default Tiles
